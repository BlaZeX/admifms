---
const { count = 8 } = Astro.props;
---
<div class="logo-grid" aria-label="Recruiter logo placeholders">{Array.from({ length: count }).map((_, index) => <div class="logo-placeholder">Approved logo {index + 1}</div>)}</div>
